IUpdatable:
	- interfejs po krórym dziedzicz¹ wszystkie klasy które przeci¹¿aj¹ metodê Update(TimeType t) w celu aktualizcji w danym czasie.

IPlugin : IUpdatable
	- interfejs, który powinnu implementowaæ wszystkie pluginy w modelu.
	
template< class Iface, class UIDType >			(IFace zawsze musi byæ typu IPlugin)
class BasePlugin : public Iface					(UIDType typ odpowiadaj¹cy za nadanie statycznego UID pluginowi)
	- klasa implemetuj¹ca czêœciowo domyœlne metody interfejsu IPlugin.
	Uwagi:
	Metody: GetTextures HasAnimatingTexture QuerySequenceAnimationSource powinny zostaæ zmienione bo teraz implemetuj¹ chwilowe rozwi¹zanie pozwalaj¹ce obs³ugiwaæ resource'y w poziomu pluginu. Zostanie to poprawione w momencie zaimplementowania poprawnego Resource managera.
	
	Membery:
		m_prevPlugin - poprzedni plugin w stosunku do akualnego w Nodzie. Pole to jest niezbêdne do poprawnego generowania geometrii i wielu innych rzeczy.
	
	Metody:
	wirtualne:
		SetGeometryChannel          	- Metody do obs³ugi pobierania i ustawiania kana³ów z pluginie. Settery domyœlnie assertuj¹. Gettery próbuj¹ pobraæ kana³ z poprzedniego pluginu
		SetTransformChannel         
		SetPixelShaderChannel       
		SetVertexShaderChannel      
		SetGeometryShaderChannel    
		GetGeometryChannel          
		GetTransformChannel         
		GetPixelShaderChannel       
		GetVertexShaderChannel      
		GetGeometryShaderChannel
		GetTextures             		- Metoda pobieraj¹ca textury. Domyœlnie zwraca pusty wektor.    
        HasAnimatingTexture         	- Metody do obs³ugi animacji. Do przerobienia razem z GetTextures
        QuerySequenceAnimationSource		
		Update							- Tego nie trzeba t³umaczyæ. Assertuje jeœli nie zostanie przeci¹¿ony.
		
	niewirtualne:
		GetName - zwraca nazwê pluginu przekazan¹ przez klasê UIDType
		
		
TODO:
		
IPixelShaderChannel:
	- interfejs kana³u shadera pixela.
IVertexShaderChannel:
	- interfejs kana³u shadera wierzcho³ka.
IGeometryShaderChannel:
	- interfejs kana³u shadera geometrii.
VertexAttributesChannel:
	- Kana³ atrybutów wierzcho³ków.
	- Zawiera wektor connected komponentów, czyli prymitywów reprezentuj¹cych kszta³ty w scenie.
	- Zawiera deskryptor opisuj¹cy typy poszczególnych danych dla wierzcho³ków. (np. xyz, uv0, uv1, ....)
	
TransformChannel:
	- Kana³ transformacji. Przechowuje wector klas bv::model::Transform.

template< class IFace, class ParameterDescriptor = EmptyParamDescriptor >
class ShaderChannel : public IFace
	- klasa implementuj¹ca bazow¹ implementacjê kana³u dowolnego shadera.
	- przechowuje kod shadera oraz zarejestrowane IValue. Bêdê one przekazane do shadera w runtime. 
	- RegisterValue( IValue * v ) funckja rejestruj¹ca IValue w ShaderChannel.
	- GetValueList pobieranie zarejetrowanych IValue z ShaderChannel
	- Update nastêpuje poprzez przeci¹zenei odpowiednie przeci¹zenie fuckcji Update w klasie pochodnej. Domyœlna implemetacja jest pusta.
	- Plugin przechowuje kana³y po swojej stronie lub pobiera je z poprzedniego pluginu w nodzie. Jeœli plugin przechowuje kana³ to przeci¹¿enie fukcji IPlugin::Update powinno wywo³ywaæ Update na przechowywanym kanale.
	- ParameterDescriptor klasa przechowuj¹ca identyfikatory parametrów shadera w statycznych memberach typu std::string.
	- SahderChannel w kontruktorze przyjmuje string ze œcie¿k¹ do pliku ze Ÿród³em shareda i wczytuje go. Kompilacja nastêpuje ju¿ po wstonie silnika po pobraniu z ShaderChannel Ÿróde³ shadera za pomoc¹ GetShaderSource.
	
Textures:

ISequenceAnimationSource:



